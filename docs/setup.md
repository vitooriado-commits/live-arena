# Setup

1. Extrai o ZIP.
2. Abre terminal na pasta.
3. Executa `npm install` e depois `npm start`.
4. Abre `http://localhost:3000`.

Nota: esta versão precisa de Node.js. Não é só GitHub Pages, porque usa servidor WebSocket.
